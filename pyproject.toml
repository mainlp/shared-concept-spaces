[tool.ruff]
lint.select = ["E", "F", "B"]
lint.ignore = ["E501"]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "obj-patch-over-time"
version = "0.1.0"
dependencies = []

[tool.setuptools]
package-dir = {"" = "src"}
